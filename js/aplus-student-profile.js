/* APLUS STUDENT PROFILE DIAGNOSTIC v4.0
   Diagnostic 01: establish the student's current academic and development baseline.
   Output is diagnostic, not an admissions prediction or ranking.
*/
(function(){
  "use strict";
  const esc=v=>String(v==null?"":v).replace(/[&<>\"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]));
  const arr=v=>Array.isArray(v)?v:[];
  const read=id=>{const e=document.getElementById(id);return e?e.value:"";};
  const split=id=>{const e=document.getElementById(id);if(id==="spWeakTopics")return Array.from(document.querySelectorAll('input[name="spWeakTopic"]:checked')).map(x=>x.value);if(e&&e.tagName==="SELECT"&&e.multiple)return Array.from(e.selectedOptions).map(o=>o.value).filter(Boolean);return read(id).split(",").map(x=>x.trim()).filter(Boolean);};
  const tax=()=>window.APLUS_SUBJECT_TAXONOMY;
  const subjectGrades={};
  const GRADE_STATE_KEY="APLUS_ACADEMIC_GRADE_STATE_V1";
  let pendingSavedProfile=null;
  function persistGradeState(){try{localStorage.setItem(GRADE_STATE_KEY,JSON.stringify(subjectGrades));}catch(e){}}
  function loadGradeState(){try{const x=JSON.parse(localStorage.getItem(GRADE_STATE_KEY)||"{}");if(x&&typeof x==="object")Object.assign(subjectGrades,x);}catch(e){}}
  function selectedSubjects(){loadGradeState();const out=[];document.querySelectorAll("#spSubjectSelector input[data-subject-key]:checked").forEach(cb=>{const level=cb.dataset.level,subject=cb.dataset.subject,category=cb.dataset.category,key=cb.dataset.subjectKey||((level+"__"+subject).replace(/[^a-z0-9]+/gi,"_"));const gradeEl=Array.from(document.querySelectorAll("#spStrengthSummary select[data-grade-key]")).find(el=>el.getAttribute("data-grade-key")===key);const liveGrade=gradeEl?gradeEl.value:"";const grade=liveGrade&&liveGrade!=="not_available"?liveGrade:(subjectGrades[key]||liveGrade||"not_available");out.push({level,subject,category,grade});});return out;}
  function renderSubjects(){const host=document.getElementById("spSubjectSelector");if(!host||!tax())return;const groups=["H2","H1","H3","CORE"];host.innerHTML=groups.map(level=>{const title=level==="CORE"?"Core / Other":"Higher "+level.slice(1);return '<div class="subject-group"><div class="subject-group-title">'+title+'</div><div class="subject-options">'+tax().list(level).map(x=>{const k=level+"__"+x.subject.replace(/[^a-z0-9]+/gi,"_");return '<div class="subject-option"><label><input type="checkbox" data-subject-key data-subject-key-value="'+k+'" data-subject-key="'+k+'" data-level="'+esc(x.level)+'" data-subject="'+esc(x.subject)+'" data-category="'+esc(x.category)+'"> '+esc(x.subject)+'</label></div>';}).join("")+'</div></div>';}).join("");host.querySelectorAll("input[data-subject-key]").forEach(cb=>cb.addEventListener("change",()=>{updateSubjectSummary();updateSubjectGrades();refreshAcademicOutputs();}));}
  function updateAcademicAnalysis(){const host=document.getElementById("spAcademicAnalysis");if(!host||!window.APLUS_ACADEMIC_ANALYSIS)return;const visibleRows=Array.from(document.querySelectorAll("#spStrengthSummary .sp-grade-row")).map(row=>{const level=row.querySelector(".sp-grade-level")?.textContent.trim()||"",subject=row.querySelector(".sp-grade-subject")?.textContent.trim()||"",grade=row.querySelector("select")?.value||"not_available";const tax=window.APLUS_SUBJECT_TAXONOMY&&window.APLUS_SUBJECT_TAXONOMY.normalizeOne?window.APLUS_SUBJECT_TAXONOMY.normalizeOne({level,subject}):null;return {level,subject,grade,category:tax?.category||"Other"};}).filter(x=>x.subject);const a=window.APLUS_ACADEMIC_ANALYSIS.analyze(visibleRows,read("spAcademic")||"unknown");host.innerHTML='<div class="sp-derived-box"><div class="sp-section-mini-title">Academic Pattern</div><b>'+esc(a.pattern.label)+'</b><div class="sp-section-mini-title" style="margin-top:12px">Academic Readiness</div><b>'+esc(a.readiness.label)+'</b><div style="font-size:11px;color:#667085;margin-top:4px">'+esc(a.readiness.reason)+'</div><div class="sp-section-mini-title" style="margin-top:12px">Academic Profile Summary</div><div style="font-size:12px;line-height:1.6;color:#475467">'+esc(a.summary)+'</div></div>';}
  function refreshAcademicOutputs(){updateSubjectSummary();updateStrengthList();updateAcademicAnalysis();updateALevelScore();}
  function updateSubjectSummary(){const el=document.getElementById("spSubjectSummary");if(!el)return;const selected=selectedSubjects();if(!selected.length){el.textContent="Select your subjects";return;}const h1=selected.filter(x=>x.level==="H1").length,h2=selected.filter(x=>x.level==="H2").length,h3=selected.filter(x=>x.level==="H3").length;const parts=[];if(h1)parts.push("H1: "+h1);if(h2)parts.push("H2: "+h2);if(h3)parts.push("H3: "+h3);el.textContent=selected.length+" subjects selected · "+parts.join(" · ");}
  function updateALevelScore(){const host=document.getElementById("spALevelScore");if(!window.APLUS_ALEVEL_SCORE||!host)return;const rows=Array.from(document.querySelectorAll("#spStrengthSummary .sp-grade-row")).map(row=>({level:row.querySelector(".sp-grade-level")?.textContent.trim()||"",subject:row.querySelector(".sp-grade-subject")?.textContent.trim()||"",grade:row.querySelector("select")?.value||"not_available"})).filter(x=>x.subject);rows.forEach(row=>{const pointEl=Array.from(document.querySelectorAll("#spStrengthSummary .sp-grade-row")).find(r=>r.querySelector(".sp-grade-level")?.textContent.trim()===row.level&&r.querySelector(".sp-grade-subject")?.textContent.trim()===row.subject)?.querySelector(".sp-grade-points");const pts=window.APLUS_ALEVEL_SCORE.points(row.level,row.grade);if(pointEl)pointEl.textContent=pts===null?"—":(Number.isInteger(pts)?pts.toFixed(1):pts.toFixed(2))+" pts";});const result=window.APLUS_ALEVEL_SCORE.calculate(rows);if(result.complete&&result.uas!==null){const counted=result.components.h2.slice();if(result.components.gp)counted.push(result.components.gp);if(result.optional?.included?.fourth&&result.optional.fourthSubject){const fourth=result.optional.fourthSubject;if(!counted.some(x=>x.level===fourth.level&&x.subject===fourth.subject))counted.push(fourth);}host.innerHTML='<div class="sp-uas-card"><div><span class="sp-uas-label">Estimated UAS</span><strong>'+result.uas.toFixed(2)+'</strong><span class="sp-uas-max">/ 70</span></div><div class="sp-uas-note">Best 3 H2 content subjects + General Paper.</div></div>';}else{const graded=rows.filter(x=>window.APLUS_ALEVEL_SCORE.points(x.level,x.grade)!==null);host.innerHTML='<div class="sp-uas-card sp-uas-pending"><div><span class="sp-uas-label">UAS</span><strong>—</strong><span class="sp-uas-max">/ 70</span></div><div class="sp-uas-note">'+graded.length+' graded subject'+(graded.length===1?"":"s")+' recorded.</div></div>';}}
  function updateSubjectGrades(){const host=document.getElementById("spStrengthSummary");if(!host)return;const selected=selectedSubjects();if(!selected.length){host.innerHTML='<div class="sp-grade-empty">Select subjects above first.</div>';return;}const gradeOptionsForLevel=level=>level==="H3"?['Distinction','Merit','Pass','Ungraded']:(tax()?tax().gradeOptions:[]);host.innerHTML='<div class="sp-grade-list">'+selected.map(x=>{const k=x.level+"__"+x.subject.replace(/[^a-z0-9]+/gi,"_");const options=['<option value="not_available">Not available yet</option>'].concat(gradeOptionsForLevel(x.level).map(g=>'<option value="'+g+'">'+g+'</option>')).join("");return '<div class="sp-grade-row"><div><span class="sp-grade-level">'+esc(x.level)+'</span><span class="sp-grade-subject">'+esc(x.subject)+'</span></div><div class="sp-grade-score"><select data-grade-key="'+esc(k)+'">'+options+'</select><span class="sp-grade-points">—</span></div></div>';}).join("")+'</div>';selected.forEach(x=>{const k=x.level+"__"+x.subject.replace(/[^a-z0-9]+/gi,"_");const el=Array.from(host.querySelectorAll("select[data-grade-key]")).find(s=>s.getAttribute("data-grade-key")===k);if(el){el.value=(subjectGrades[k]&&subjectGrades[k]!=="not_available")?subjectGrades[k]:(x.grade||"not_available");subjectGrades[k]=el.value;el.addEventListener("change",async()=>{subjectGrades[k]=el.value||"not_available";persistGradeState();refreshAcademicOutputs();try{if(window.APLUS_DATABASE&&window.APLUS_DATABASE.saveAcademicGrades){const rows=selectedSubjects().map(s=>{const kk=s.level+"__"+String(s.subject||"").replace(/[^a-z0-9]+/gi,"_");return {level:s.level,subject:s.subject,grade:subjectGrades[kk]||"not_available"};});await window.APLUS_DATABASE.saveAcademicGrades(rows);}}catch(e){console.warn("Academic grade autosave failed:",e);}});}});updateALevelScore();}
  function updateStrengthList(){const host=document.getElementById("spStrengthList");if(!host)return;const rows=Array.from(document.querySelectorAll("#spStrengthSummary .sp-grade-row"));const selected=rows.map(row=>({level:row.querySelector(".sp-grade-level")?.textContent.trim()||"",subject:row.querySelector(".sp-grade-subject")?.textContent.trim()||"",grade:row.querySelector("select")?.value||"not_available"})).filter(x=>x.subject);if(!selected.length){host.innerHTML='<div class="sp-grade-empty">Select subjects and grades above to identify the academic profile.</div>';return;}host.innerHTML='<div class="sp-strength-list">'+selected.map(x=>'<span class="sp-strength-chip"><b>'+esc(x.level)+'</b> '+esc(x.subject)+' <em>'+esc(x.grade)+'</em></span>').join("")+'</div>';}
  function syncCurrentGradeControls(){document.querySelectorAll("#spStrengthSummary select[data-grade-key]").forEach(el=>{const key=el.getAttribute("data-grade-key");if(key)subjectGrades[key]=el.value||"not_available";persistGradeState();});}
  function collect(){loadGradeState();syncCurrentGradeControls();let activities=[];try{const saved=JSON.parse(localStorage.getItem("aplus_experience_profile_v1")||"[]");activities=Array.isArray(saved)?saved:[]}catch(e){}const raw={field:read("spTargetField")||"Medicine",university:read("spTargetUniversity")||"Not decided yet",course:read("spTargetField")||"Medicine",country:"Singapore",entryYear:read("spEntryYear")||"2027",scholarship:read("targetScholarship"),currentLevel:read("spLevel")||"Not specified",qualification:read("spQualification"),academicProfile:read("spAcademicProfile"),subjects:selectedSubjects(),strengths:[],weakTopics:split("spWeakTopics"),readiness:{academic:"unknown",test:"unknown",communication:"unknown",leadership:"unknown",service:"unknown",application:"unknown"},activities};const base=window.APLUS_MASTER_PROFILE.create(raw);base.studentName=read("studentName")||"";const graded=base.profile.subjects.filter(x=>x.grade&&x.grade!=="not_available");base.profile.academicAnalysis=window.APLUS_ACADEMIC_ANALYSIS?window.APLUS_ACADEMIC_ANALYSIS.analyze(base.profile.subjects,read("spAcademic")||"unknown"):base.profile.academicAnalysis;base.profile.strengths=graded.filter(x=>["A","B"].includes(x.grade)).map(x=>x.level+" "+x.subject);base.profile.weakTopics=split("spWeakTopics");base.schemaVersion="4.0";return base;}
  function diagnosticState(base){const p=base.profile||{},subjects=arr(p.subjects),graded=subjects.filter(x=>x.grade&&x.grade!=="not_available");return {state:{education:p.currentLevel?"recorded":"unknown",qualification:p.qualification?"recorded":"unknown",subjects:subjects.length?"recorded":"unknown",grades:graded.length?"recorded":"unknown"},next:graded.length?[]:["Add the latest available subject grades."]};}
  function save(profile){try{localStorage.setItem("APLUS_MASTER_PROFILE",JSON.stringify(profile));localStorage.setItem("APLUS_DIAGNOSTIC_01",JSON.stringify({version:"5.0",completedAt:new Date().toISOString(),state:diagnosticState(profile).state}));}catch(e){}}
  function hydrateSavedProfile(row){
    try{
      if(!document.getElementById("spSubjectSelector")||!document.getElementById("spStrengthSummary")){pendingSavedProfile=row;setTimeout(function(){if(pendingSavedProfile){const p=pendingSavedProfile;pendingSavedProfile=null;hydrateSavedProfile(p);}},150);return true;}
      const ap=(row&&row.academic_profile)||{};
      const savedGradeRows=Array.isArray(ap.subjectGrades)?ap.subjectGrades:[];
      const savedGradeMap=new Map(savedGradeRows.map(x=>[String(x.level||"")+"::"+String(x.subject||"").toLowerCase(),x.grade]));
      const subjects=arr(ap.subjects).map(x=>{const key=String(x.level||"")+"::"+String(x.subject||"").toLowerCase();const g=savedGradeMap.get(key);return Object.assign({},x,(g&&g!=="not_available")?{grade:g}:{});});
      const set=(id,value)=>{const el=document.getElementById(id);if(el&&value!=null&&value!=="")el.value=String(value);};
      set("spQualification",row&&row.qualification);set("spLevel",ap.currentLevel);set("spAcademicProfile",ap.academicProfile);
      subjects.forEach(x=>{const key=String(x.level||"")+"__"+String(x.subject||"").replace(/[^a-z0-9]+/gi,"_");subjectGrades[key]=x.grade||"not_available";const cb=document.querySelector('#spSubjectSelector input[data-subject-key="'+key+'"]');if(cb)cb.checked=true;});
      updateSubjectSummary();updateSubjectGrades();
      if(ap.weakTopics&&Array.isArray(ap.weakTopics))document.querySelectorAll('input[name="spWeakTopic"]').forEach(cb=>{cb.checked=ap.weakTopics.includes(cb.value);});
      const merged=Object.assign({},(()=>{try{return JSON.parse(localStorage.getItem("APLUS_MASTER_PROFILE")||"{}")}catch(e){return {}}})(),{profile:Object.assign({},ap,{subjects}),studentName:row&&row.display_name||""});localStorage.setItem("APLUS_MASTER_PROFILE",JSON.stringify(merged));
      refreshAcademicOutputs();persistGradeState();return true;
    }catch(e){console.warn("hydrateSavedProfile failed",e);return false;}
  }
  window.APLUS_BUILD_STUDENT_PROFILE=build;
  window.APLUS_STUDENT_PROFILE={collect,save,build,diagnosticState,hydrateSavedProfile};
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",inject);else inject();
})();
